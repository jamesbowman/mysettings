for x in .vimrc .screenrc .bash_aliases
do
  ln -s $PWD/$x $HOME
done
mkdir -p $HOME/.i3
cp config $HOME/.i3/

git config --global user.email jamesb@excamera.com
git config --global user.name "James Bowman"
git config --global push.default matching

